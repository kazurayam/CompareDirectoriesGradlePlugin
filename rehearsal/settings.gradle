// include the build of the neighbor 'plugin' project
includeBuild("../plugin") {
    dependencySubstitution {
        // explicitly load the output of the included build
        // into the consumer project's classpath
        substitute(module('com.kazurayam:compare-directories'))
    }
}
